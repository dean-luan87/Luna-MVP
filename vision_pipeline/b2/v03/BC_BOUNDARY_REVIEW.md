# B/C 边界复审报告

## 🎯 复审目标

**检查 B/C 是否还有"隐性越权点"**

基于已裁定的 7 条边界假设，系统性地检查代码中可能存在的隐性越权。

---

## 📋 复审清单

### 1. B 是否在"建议"中隐含"命令"？

**检查点：**
- B 的输出是否使用"应该"、"必须"等命令性语言
- B 的 confidence 是否被 C 误读为"确定性"

**发现：**
- ✅ `advisory_only = True` 已强制声明
- ✅ `intervention_level` 已明确区分 HARD/SOFT
- ⚠️ 需要检查 `_build_message_to_c()` 中的语言表述

**建议：**
- 在 payload 中明确标注 `"type": "suggestion"`（除 NEED_STOP 外）

---

### 2. C 是否可能"反向请求"B 的确认？

**检查点：**
- C 是否有可能向 B 发送"请确认风险"的请求
- B 是否有可能响应这种请求

**发现：**
- ✅ 当前代码中 B 是单向输出，无接收 C 消息的机制
- ⚠️ 未来如果添加 B←C 通信，需要明确禁止"确认请求"

**建议：**
- 在 `bc_boundary_assumptions_v1.md` 中明确：B 不接受 C 的"确认请求"

---

### 3. Gate 是否可能"间接影响"C？

**检查点：**
- Gate 的状态是否可能通过其他路径影响 C
- Gate 的 reason 是否可能被 C 读取并用于决策

**发现：**
- ✅ Gate 状态只写入 trace，不进入 B→C 消息
- ✅ `gate_runtime.py` 明确 Gate 只影响 B
- ⚠️ 需要确认 trace 中的 gate 信息不会被 C 读取

**建议：**
- 在文档中明确：C 不应读取 B 的 trace 中的 gate 信息

---

### 4. 时间语义是否可能被"误用"？

**检查点：**
- `system_ts` 是否可能被 C 误读为"预测时间"
- `valid_until` 是否可能被 C 理解为"承诺时间"

**发现：**
- ✅ `system_ts` 已强制使用
- ⚠️ `valid_until` 可能被误解为"承诺在此时间前有效"

**建议：**
- 将 `valid_until` 重命名为 `suggested_validity_window` 或添加注释说明这是"建议窗口，非承诺"

---

### 5. NO_OP 的"沉默"是否可能被误读？

**检查点：**
- C 是否可能将"B 沉默"理解为"B 确认安全"
- 系统是否可能将"无输出"理解为"无风险"

**发现：**
- ✅ `decision_state = "SILENT"` 已明确标记
- ✅ `silence_reason` 已记录原因
- ⚠️ 需要确认 C 不会将"无消息"理解为"确认安全"

**建议：**
- 在 B→C 消息协议中明确：无消息 ≠ 确认安全

---

### 6. DCS 是否可能"反向影响"决策？

**检查点：**
- DCS 的检查结果是否可能被用于修改决策
- DCS 的 penalty 是否可能影响 B 的输出

**发现：**
- ✅ `dcs_guard.py` 中 `dcs_check()` 只读 summary，不修改
- ✅ DCS 结果只写入 trace，不进入决策流程
- ✅ 已验证：DCS 不修改 summary

**结论：** ✅ 无隐性越权

---

### 7. 角色声明是否可能被"忽略"？

**检查点：**
- `role = "B"` 是否可能被 C 忽略
- `expects_confirmation_from = "C"` 是否可能被 C 忽略

**发现：**
- ✅ 角色声明已添加到 summary
- ⚠️ 需要确认 C 的代码会检查这些字段

**建议：**
- 在 C 的代码中添加验证：如果收到 B 消息，必须检查 `role == "B"`

---

### 8. 干预级别的"唯一性"是否可能被绕过？

**检查点：**
- 是否可能通过其他字段实现"二级干预"
- 是否可能通过 confidence 值实现"隐式强制"

**发现：**
- ✅ `intervention_level` 已强制设置
- ⚠️ 需要检查是否有其他字段可能被误用为"强制信号"

**建议：**
- 在 B→C 消息协议中明确：只有 `intervention_level == "HARD"` 且 `impact == "NEED_STOP"` 才是干预

---

### 9. 频率差异是否可能被"抹平"？

**检查点：**
- B 是否可能尝试"实时响应"
- C 是否可能尝试"长期预测"

**发现：**
- ✅ B 的延迟特性已在设计文档中明确
- ⚠️ 需要检查代码中是否有"实时优化"的逻辑

**建议：**
- 在代码注释中明确：B 不应优化延迟，延迟是设计特性

---

### 10. 进化方向的"分离"是否可能被破坏？

**检查点：**
- B 是否可能尝试"实时执行"
- C 是否可能尝试"未来建模"

**发现：**
- ✅ 当前代码中 B 只做预测，C 只做执行
- ⚠️ 需要防止未来"功能迁移"

**建议：**
- 在架构文档中明确：B 和 C 的进化方向必须正交

---

## 🎯 发现的隐性越权点

### 高风险（需要立即修复）

**无**

### 中风险（需要文档明确）

1. **`valid_until` 可能被误解为承诺**
   - 建议：重命名或添加明确注释

2. **C 可能将"无消息"理解为"确认安全"**
   - 建议：在 B→C 协议中明确：无消息 ≠ 确认安全

3. **C 可能忽略角色声明**
   - 建议：在 C 代码中添加角色验证

### 低风险（需要监控）

1. **未来 B←C 通信可能引入"确认请求"**
   - 建议：在边界假设中明确禁止

2. **Gate 信息可能被 C 间接读取**
   - 建议：在文档中明确 C 不应读取 B 的 trace

---

## ✅ 复审结论

### 当前代码状态

- ✅ **无高风险隐性越权点**
- ⚠️ **3 个中风险点需要文档明确**
- ⚠️ **2 个低风险点需要监控**

### 建议的补充措施

1. **立即补充：**
   - 在 B→C 消息协议中明确：无消息 ≠ 确认安全
   - 将 `valid_until` 重命名为更明确的名称或添加注释

2. **文档明确：**
   - 在 `bc_boundary_assumptions_v1.md` 中明确禁止 B←C 的"确认请求"
   - 在文档中明确 C 不应读取 B 的 trace

3. **代码验证：**
   - 在 C 代码中添加角色验证逻辑
   - 在 C 代码中添加"无消息 ≠ 确认安全"的处理

---

## 📝 复审完成状态

**状态：** ✅ **复审完成**

**结论：** 当前代码无高风险隐性越权点，但需要补充文档明确和代码验证。

---

**复审日期：** 2025-01-12  
**复审人：** AI Assistant  
**基于文档：** `bc_boundary_assumptions_v1.md`
